version: '3.8'

services:
  cox-hazard-ratio-calculator:
    build: .
    container_name: cox-hazard-ratio-calculator
    ports:
      - "8000:8000"
    environment:
      - AUDIT_SECRET_KEY=cox-hazard-ratio-calculator-prod-audit-key-2026
      - MODEL_PROVIDER=mock
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
